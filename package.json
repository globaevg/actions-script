{
  "name": "actions-scripts-ui",
  "version": "1.0.0",
  "private": true,
  "description": "A simple dependency-free UI project that runs with one command.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
