# Actions Scripts UI

A small dependency-free UI project with a local Node server.

## Run

```bash
npm start
```

Then open `http://localhost:5173`.

You can choose a different port:

```bash
PORT=3000 npm start
```
